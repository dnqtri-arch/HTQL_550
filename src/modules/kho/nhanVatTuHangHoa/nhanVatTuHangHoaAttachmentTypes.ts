/** File đính kèm phiếu nhận vật tư hàng hóa — NVTHH (cấu trúc tương thích JSON lưu cục bộ). */
export interface NhanVatTuHangHoaAttachmentItem {
  name: string
  data: string
  saved_at?: string
  virtual_path?: string
}
